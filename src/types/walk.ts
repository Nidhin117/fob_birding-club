export interface Walk {
  id: string;
  title: string;
  location: string;
  date: string;
  description?: string;
  speciesCount?: number;
  participantCount?: number;
  ebirdUrl?: string;
  registrationUrl?: string;
  image?: string;
}
